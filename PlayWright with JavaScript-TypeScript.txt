--Features of PlayWright: 
  ----------------------
  -PlayWright has Auto-wait capability
  -It supports all major browser
  -Multiplatform
  -Multi-Lingual Flexibility --> JavaScript, TypeScript, Java, Python etc

Note: Action for which autowait functionality --> https://playwright.dev/docs/actionability

 -Advanced Features:
  -----------------
  -Tracing and Debuging(Built-in capability): automatic screenshots, test video recordings and comprehensive logging
  -Network Interception: API testing library intercepts and validate network calls (test edge case scenarios)
  -Browser context management: Browser context feature allows you to save and transfer broser state across your test suite, 
                              improving test efficiency and reducing overhead of setting up same browser state for each Test Case
							  (For this to happen, take cookies/storage info from logged in browser and copy into incognito if you open icongnito
							  so that it can log their too.)
  -Codegen tool: Generates test code by recording your actions
  
--Install Node.js and VS Code by setting up playwright environment:
  ----------------------------------------------------------------
  Node.js runs on V8 engine
  -Install Node.js
  -Install VS Code
  -Documentation: https://playwright.dev
  -After installation of Node.js --> Go to C:\Program Files\nodejs --> Copy Path
                                 --> Go to System Environmental Properties --> Environment variable --> System Variables --> New
								     Paste  Path by creating variable--> NODE_HOME
  -Install playwright dependencies --> npm init playwright@latest
                                   It will ask: TypeScript or JavaScript → choose what you prefer
								                Install browsers? → Yes
												Add GitHub Actions? → Optional
												This automatically installs: Playwright,Browsers (Chromium, Firefox, WebKit),Example tests,Config file
												
--PlayWright Automation Core Concepts:
  -----------------------------------
  Steps: Open VS Code--> Create Folder --> Install Playwright dependency via terminal (Answer Questions:
                                                             JS/TS, Where to put End to end tests? --> tests(folder), Adde Github Actions workflow-->true)
		 Go to Folder--> playwright.config.js --> This is TestRunner for entire project test cases
		                 package.json --> Default file created for every node project, contains info about playwright dependencies
		 tests folder--> example.spec.js --> test cases demo
		 node-module folder--> playwright
		 
--Importance of PlayWright Test Annotation and async await understanding:
  ----------------------------------------------------------------------
  Go to test folder --> Create a file = UIBasictest.spec.ts
                        To import annoation @playwright/test --> require('@playwright/test') i.e need to use this line

-JavaScript is async --> All steps try to execute at a time, need to explicitly i.e await for every step 
                      -> To activate this await, function should be marked as async
ex:						
const { test } = require('@playwright/test')
//These test are executed to launch the browser and provide a fresh page to each test
//to write new test write test() which takes two parameters, first is the name of the test,
// and second is an async function which contains the test steps
test('First playwright test', async function() {
  //Playwrright code
});

Also we can use Anonymous function
const { test } = require('@playwright/test')
//These test are executed to launch the browser and provide a fresh page to each test
//to write new test write test() which takes two parameters, first is the name of the test,
// and second is an async function which contains the test steps
test('First playwright test',async () => {
  //Playwrright code
});

--Browser context and page fixtures: page fixtures are global variables, total 4 fixtures available in playwright
  ---------------------------------
  fixtures wrap in curly braces so that it will understand that it is playwright fixture. e.g {browser}
  -browser is a fixture, globally available -> for each and every tests, need to involve browser, so we need to send as parameter of test function, and it passed inside body.
   browser.newContext() --> Need to open fresh browser i.e fresh instance(like incognito), it starts browser with fixed properties. Also we can inject cookies inside method parameter
                            So playwright will open fresh browser(with inbuilt proxy or pinging) with injected cookies
   context.newPage() --> To open new page, it will creates actual page to automate.hit url and start automation
   page.goto("https://rahulshettyacademy.com/loginpagePractise/") --> If we want to hit url
   
  -If you don't want to configure context, then their is another fixture --> async({ browser, page }) 
       i.e we don't have any special(i.e cookies or proxy etc) to inject in browser, we want blank context i.e default context and page will be given by playwright
ex:
test("Browser context declareation", async ({browser}) =>
{
    //chrome plugins or cookies
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto("https://rahulshettyacademy.com/loginpagePractise/");
})

--Playwright configuration file and its details to run the tests: --> playwright.config.js refer this file
  --------------------------------------------------------------
  contains details about what tests to run, what browser to trigger, timeouts
  -testDir --> test Directory --> To tell what tests are present --> This will triggers ./tests/___ ->also particular tests we can add.
  -timeOuts -> by default for each test 30 sec, also their is expect timeout, for Assertion validation
               We can override in defineConfig{}
  -what browser you want to run will be provided in -->  use:{}
  use: {
    /* Base URL to use in actions like `await page.goto('')`. */
    // baseURL: 'http://localhost:3000',
    browserName: 'chromium',
    /* Collect trace when retrying the failed test. See https://playwright.dev/docs/trace-viewer */
    trace: 'on-first-retry',
  },

change file like 
const config = defineConfig({
   //config holding all info required to run the test
});
module.exports = config;    //export so that it will be available across all the project

  -by default playwright run tests in headless mode, so to tell it run in head mode
    use: {
    /* Base URL to use in actions like `await page.goto('')`. */
    // baseURL: 'http://localhost:3000',
    browserName: 'chromium',      //Here we can change browser
    headless: false,
    /* Collect trace when retrying the failed test. See https://playwright.dev/docs/trace-viewer */
    trace: 'on-first-retry',
  },
  
  -To run all tests ---> npx playwright test --> It will find config and execute(In node-moules, their is playwright module)
                    ---> npx playwright test --headed  (run in terminal)
Note: To trigger only one test from file and skip other --> Use test.only()

--Setup Assertions and Multiple browser configuration:
  ---------------------------------------------------
  -https://playwright.dev/docs/test-assertions
ex: 
const { test,expect } = require('@playwright/test')
//These test are executed to launch the browser and provide a fresh page to each test
//to write new test write test() which takes two parameters, first is the name of the test,
// and second is an async function which contains the test steps
test('First playwright test',async () => {
  //Playwrright code
});

test("Browser context declareation", async ({browser}) =>
{
    //chrome plugins or cookies
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto("https://rahulshettyacademy.com/loginpagePractise/");
})

test("page playwright test", async ({page}) =>{
    await page.goto("https://www.google.com/");
    //get title assertion
    console.log(await page.title());
    await expect(page).toHaveTitle("Google");
})

Note: Their are two types of timeout --> timeOut: 30*1000; -->Global timeOut
                                     --> expect: {
									        timeOut:5000   -->Assertion
										}

--Playwright Basic methods for Web Automation Test:
  ------------------------------------------------
 -Playwright support CSS Selector, xpath (Predominantly supports css)
 -page.locator() --> to locate element
 -Rules to write CSS Selector:
  ---------------------------
  -If id  is present --> CSS --> tagName#id or #id
  -If class attribute is present --> CSS --> tagName.class or .class
  -Write CSS based on any attribute --> CSS --> [attribute = 'value']
  -Write CSS with traversing from parent to child --> CSS --> parenttagName>>childtagName
  -If nees to write locator based on text --> text = ''
  
-----------------------------------------
|              SIGN IN                 |
-----------------------------------------
| Username : [____________________]     |          //To input value in edit box --> fill() --> also can be used to clear field --> fill("")
| Password : [____________________]     |          //earlier we used type(), but now it is deprecated
|                                       |
| Role:  ( ) Admin   ( ) User          |
|                                       |
| [ ] I Agree                          |
|                                       |
|        [   Sign In   ]               |
-----------------------------------------

-Extracting the text from browser and inserting valid expect assertion in test:
 -----------------------------------------------------------------------------
 https://rahulshettyacademy.com/loginpagePractise/
 Scenario: When user inpt wrong credential and click on sign in --> alert displays and quickly vanishes
 style="display: none;" -> changes to -> style="display: block;" --> we get error message  as 'Incorrect Username/Password' or ('Empty username/password.' in case of empty)
 To find error message --> [style*='block'] --> Here * means, we can give it partially attribute value.
 Playwright has intelligence-->If element is not present on page, it will wait few second to decide, to make sure and it's displaying or not
 textContent() --> To extract text from element or locator
eg:
test.only("Brwoser context playwright test", async({browser}) =>{
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto("https://rahulshettyacademy.com/loginpagePractise/");
    console.log(await page.title());
    //await page.locator('#username').type("rahulshetty");
    await page.locator('#username').fill("rahulshetty");
    await page.locator('#password').fill("learning");
    await page.locator('#signInBtn').click();
    console.log(await page.locator("[style*='block']").textContent());
    await expect(page.locator("[style*='block']")).toContainText("Incorrect");
})

-Locators to extract multiple webElements in page:
 ------------------------------------------------
test.only("Browser context-validating Error login", async({ browser }) =>{
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto("https://rahulshettyacademy.com/loginpagePractise/");
    console.log(await page.title());
    const username = page.locator('#username');
    const password = page.locator('#password');
    const signInBtn = page.locator('#signInBtn');
    await username.fill("rahulshetty");
    await password.fill("Learning@830$3mK2");
    await signInBtn.click();
    console.log(await page.locator("[style*='block']").textContent());
    await expect(page.locator("[style*='block']")).toContainText("Incorrect");
    await username.fill("");
    await username.fill("rahulshettyacademy");
    await signInBtn.click();
    //console.log(await page.locator(".card-body a").textContent()); //Strict mode violation, multiple element will be found
    //console.log(await page.locator(".card-body a").first().textContent());  //This works
    console.log(await page.locator(".card-body a").nth(0).textContent()); //This also works
})

--How wait mechanism works if list of elements are returned:
  ---------------------------------------------------------
test.only("Browser context-validating Error login", async({ browser }) =>{
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto("https://rahulshettyacademy.com/loginpagePractise/");
    console.log(await page.title());
    const username = page.locator('#username');
    const password = page.locator('#password');
    const signInBtn = page.locator('#signInBtn');
    await username.fill("rahulshetty");
    await password.fill("Learning@830$3mK2");
    await signInBtn.click();
    console.log(await page.locator("[style*='block']").textContent());
    await expect(page.locator("[style*='block']")).toContainText("Incorrect");
    await username.fill("");
    await username.fill("rahulshettyacademy");
    await signInBtn.click();
    //console.log(await page.locator(".card-body a").textContent()); //Strict mode violation, multiple element will be found
    //console.log(await page.locator(".card-body a").first().textContent());  //This works
    //console.log(await page.locator(".card-body a").nth(0).textContent()); //This also works
    const cardTitles = page.locator(".card-body a");
    console.log(await cardTitles.first().textContent());  //for textContent() --> Page will wait  until locator is attached to dom.
    const allTitles = await cardTitles.allTextContents(); //for allTextContent() -->returns array and  page don't wait, as no synchronization provided in playwright 
    // so if we comment previous line test will fail. --> will return empty array
    console.log(allTitles);
})

Note: For single element, if not found--> It understands may be page is not loaded, so let us wait(Attached mode activated) --> But List can be empty or 0 value 

--Techniques to wait dynamically for new page in service based application:
  ------------------------------------------------------------------------
const { expect, test } = require("@playwright/test");
test.only("Validating wait mechanism in playwright", async ({ browser }) => {
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto("https://rahulshettyacademy.com/client");
    await page.locator('#userEmail').fill("rohit.lavate287@gmail.com");
    await page.locator('#userPassword').fill('Admin@123');
    await page.locator("[value='Login']").click();
    //Implement wait mechanism --> In network all data is driven by API calls/Service calls, 
    // so we can wait for API call to get completed and then move forward with next step (Fetch/xhr)
    //await page.waitForLoadState('networkidle'); //wait for network to be idle, means all API calls are completed
    //but above line is not recommended in official documentation, as it will wait for all API calls to get completed, 
    // It may fail sometimes
    //so solution is use waitFor() method --> It will wait for specific Single element, not for multiple
    await page.locator(".card-body b").first().waitFor(); //wait for first element to be visible
    const titles = await page.locator(".card-body b").allTextContents();
    console.log(titles);
})

--Handling UI Components: --> DropDown, Radio Buttons, Child Windows etc
  ----------------------
 -Handling static select dropdown options with playwright:
  -------------------------------------------------------
const {test, expect} = require('@playwright/test');
test.only("Handling Select dropdown in playwright", async ({browser}) =>{
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto("https://rahulshettyacademy.com/loginpagePractise/");
    const username = page.locator('#username');
    const signIn = page.locator('#signInBtn');
    await username.fill("rahulshettyacademy");
    const dropdown = page.locator('select.form-control');
    await dropdown.selectOption("Teacher");
    //await page.pause(); //This will pause the test execution and open the playwright inspector,
    //  where we can interact with the page and see the test steps
    await page.locator('.radiotextsty').last().click(); 
    //first() --> will select first element, last() --> will select last element, 
    // nth(1) --> will select second element. These are used when multiple elements are found for the locator,
    //  and we want to select specific element.
    await page.locator('#okayBtn').click();
    await expect(page.locator('.radiotextsty').last()).toBeChecked(); //toBeChecked() --> will check if the element is checked or not, it will return true or false
    //console.log(await expect(page.locator('.radiotextsty').last()).isChecked()); //isChecked() --> will check if the element is checked or not, it will return true or false
    await page.locator('#terms').click();
    await expect(page.locator('#terms')).toBeChecked();
    //await page.locator('.terms').waitFor({ state: 'visible', timeout: 5000 });
    await page.locator('#terms').uncheck(); //uncheck() --> will uncheck the checkbox, if it is already checked, if it is not checked, it will do nothing
    expect(await page.locator('#terms').isChecked()).toBeFalsy(); 
    //toBeFalsy() --> will check if the element is not checked, it will return true, if it is checked, it will return false
    //toBeTruthy() --> will check if the element is checked, it will return true, if it is not checked, it will return false
});